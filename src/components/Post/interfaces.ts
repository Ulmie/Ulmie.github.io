export type PostProps = {
  post: {
    author?: string;
    creator: Array<string>;
    pubDate?: string;
    title?: string;
    description?: string;
    content?: string;
    image_url?: string;
  }
};